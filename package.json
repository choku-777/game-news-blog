{
  "name": "game-news-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "build": "hugo --minify"
  },
  "dependencies": {
    "rss-parser": "^3.13.0",
    "jsdom": "^24.0.0",
    "@mozilla/readability": "^0.5.0"
  }
}
